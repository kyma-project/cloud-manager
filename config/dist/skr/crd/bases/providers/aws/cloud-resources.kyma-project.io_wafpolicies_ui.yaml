apiVersion: v1
data:
  details: |
    body:
        - name: configuration
          widget: Panel
          source: spec
          children:
            - widget: CodeViewer
              source: data
              name: spec.data
              language: json
        - name: status
          widget: Panel
          source: status
          children:
            - widget: Labels
              source: providerId
              name: status.providerId
            - widget: Conditions
              source: conditions
              name: status.conditions
  form: |
    - path: spec.data
      name: spec.data
      widget: CodeEditor
      language: "'plaintext'"
      required: true
  general: |
    resource:
        kind: WafPolicy
        group: cloud-resources.kyma-project.io
        version: v1beta1
    urlPath: wafpolicies
    name: WAF Policies
    scope: cluster
    category: Security
    description: >-
        Web Application Firewall (WAF) policies for protecting web applications
  list: |
    - source: status.providerId
      name: status.providerId
  translations: |
    en:
      spec.data: WebACL Configuration (JSON)
      status.providerId: Provider ID
      status.conditions: Status Conditions
      configuration: Configuration
      status: Status
kind: ConfigMap
metadata:
  annotations:
    cloud-resources.kyma-project.io/version: v0.0.1
  labels:
    busola.io/extension: resource
    busola.io/extension-version: "0.5"
    cloud-manager: ui-cm
  name: wafpolicies-ui.operator.kyma-project.io
  namespace: kyma-system
