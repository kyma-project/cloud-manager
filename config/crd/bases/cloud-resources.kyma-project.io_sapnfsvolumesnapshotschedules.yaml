---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
    cloud-resources.kyma-project.io/version: v0.0.1
  name: sapnfsvolumesnapshotschedules.cloud-resources.kyma-project.io
spec:
  group: cloud-resources.kyma-project.io
  names:
    categories:
      - kyma-cloud-manager
    kind: SapNfsVolumeSnapshotSchedule
    listKind: SapNfsVolumeSnapshotScheduleList
    plural: sapnfsvolumesnapshotschedules
    singular: sapnfsvolumesnapshotschedule
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .spec.schedule
          name: Schedule
          type: string
        - jsonPath: .status.lastCreateRun
          name: Last Run Time
          type: date
        - jsonPath: .status.state
          name: State
          type: string
      name: v1beta1
      schema:
        openAPIV3Schema:
          description: SapNfsVolumeSnapshotSchedule is the Schema for the sapnfsvolumesnapshotschedules API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: SapNfsVolumeSnapshotScheduleSpec defines the desired state of SapNfsVolumeSnapshotSchedule
              properties:
                deleteCascade:
                  default: false
                  description: DeleteCascade specifies whether to cascade delete the snapshots when this schedule is deleted.
                  type: boolean
                endTime:
                  description: |-
                    EndTime specifies the time when the schedule should stop creating snapshots.
                    If not provided, schedule will run indefinitely.
                  format: date-time
                  type: string
                maxFailedSnapshots:
                  default: 5
                  description: MaxFailedSnapshots is the maximum number of Failed snapshots to keep.
                  minimum: 1
                  type: integer
                maxReadySnapshots:
                  default: 50
                  description: MaxReadySnapshots is the maximum number of Ready snapshots to keep.
                  minimum: 1
                  type: integer
                maxRetentionDays:
                  default: 375
                  description: |-
                    MaxRetentionDays specifies the maximum number of days to retain snapshots.
                    Stamped as deleteAfterDays on each created snapshot.
                    If not provided, it will be defaulted to 375 days.
                  minimum: 1
                  type: integer
                nfsVolumeRef:
                  description: NfsVolumeRef specifies the source SapNfsVolume resource.
                  properties:
                    apiVersion:
                      description: API version of the referent.
                      type: string
                    fieldPath:
                      description: |-
                        If referring to a piece of an object instead of an entire object, this string
                        should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                        For example, if the object reference is to a container within a pod, this would take on a value like:
                        "spec.containers{name}" (where "name" refers to the name of the container that triggered
                        the event) or if no container name is specified "spec.containers[2]" (container with
                        index 2 in this pod). This syntax is chosen only to have some well-defined way of
                        referencing a part of an object.
                      type: string
                    kind:
                      description: |-
                        Kind of the referent.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                      type: string
                    name:
                      description: |-
                        Name of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                    namespace:
                      description: |-
                        Namespace of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                      type: string
                    resourceVersion:
                      description: |-
                        Specific resourceVersion to which this reference is made, if any.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                      type: string
                    uid:
                      description: |-
                        UID of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                prefix:
                  description: |-
                    Prefix is used as a prefix for snapshot names.
                    If not provided, schedule name will be used as prefix.
                  type: string
                schedule:
                  description: Schedule is a cron expression. If empty, creates a one-time snapshot.
                  type: string
                startTime:
                  description: |-
                    StartTime specifies the time when the schedule should start creating snapshots.
                    If not provided, schedule will start immediately.
                  format: date-time
                  type: string
                suspend:
                  default: false
                  description: Suspend stops the schedule from creating new snapshots.
                  type: boolean
                template:
                  description: Template defines the SapNfsVolumeSnapshot to create on each run.
                  properties:
                    annotations:
                      additionalProperties:
                        type: string
                      description: Annotations to apply to created SapNfsVolumeSnapshot objects.
                      type: object
                    labels:
                      additionalProperties:
                        type: string
                      description: |-
                        Labels to apply to created SapNfsVolumeSnapshot objects.
                        Merged with schedule-managed labels.
                      type: object
                    spec:
                      description: Spec is the template for the SapNfsVolumeSnapshot spec.
                      properties:
                        deleteAfterDays:
                          description: |-
                            DeleteAfterDays specifies the number of days after which the snapshot
                            will be automatically deleted. 0 means no automatic deletion.
                          type: integer
                        sourceVolume:
                          description: SourceVolume references the SapNfsVolume to snapshot.
                          properties:
                            name:
                              description: Name specifies the name of the SapNfsVolume resource.
                              type: string
                            namespace:
                              description: |-
                                Namespace specifies the namespace of the SapNfsVolume resource.
                                If not specified then namespace of the parent resource is used.
                              type: string
                          required:
                            - name
                          type: object
                          x-kubernetes-validations:
                            - message: SourceVolume is immutable.
                              rule: (self == oldSelf)
                      required:
                        - sourceVolume
                      type: object
                  required:
                    - spec
                  type: object
              required:
                - nfsVolumeRef
                - template
              type: object
            status:
              description: SapNfsVolumeSnapshotScheduleStatus defines the observed state of SapNfsVolumeSnapshotSchedule
              properties:
                backupCount:
                  default: 0
                  description: BackupCount specifies the number of snapshots currently present in the system.
                  type: integer
                backupIndex:
                  default: 0
                  description: BackupIndex specifies the current index of the snapshot created by this schedule.
                  type: integer
                conditions:
                  description: List of status conditions
                  items:
                    description: Condition contains details for one aspect of the current state of this API Resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is the last time the condition transitioned from one status to another.
                          This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message is a human readable message indicating details about the transition.
                          This may be an empty string.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          observedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          reason contains a programmatic identifier indicating the reason for the condition's last transition.
                          Producers of specific condition types may define expected values and meanings for this field,
                          and whether the values are considered a guaranteed API.
                          The value should be a CamelCase string.
                          This field may not be empty.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                  x-kubernetes-list-map-keys:
                    - type
                  x-kubernetes-list-type: map
                lastCreateRun:
                  description: LastCreateRun specifies the time when the last snapshot was created.
                  format: date-time
                  type: string
                lastCreatedBackup:
                  description: LastCreatedBackup contains the object reference of the snapshot created during last run.
                  properties:
                    apiVersion:
                      description: API version of the referent.
                      type: string
                    fieldPath:
                      description: |-
                        If referring to a piece of an object instead of an entire object, this string
                        should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                        For example, if the object reference is to a container within a pod, this would take on a value like:
                        "spec.containers{name}" (where "name" refers to the name of the container that triggered
                        the event) or if no container name is specified "spec.containers[2]" (container with
                        index 2 in this pod). This syntax is chosen only to have some well-defined way of
                        referencing a part of an object.
                      type: string
                    kind:
                      description: |-
                        Kind of the referent.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                      type: string
                    name:
                      description: |-
                        Name of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                    namespace:
                      description: |-
                        Namespace of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                      type: string
                    resourceVersion:
                      description: |-
                        Specific resourceVersion to which this reference is made, if any.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                      type: string
                    uid:
                      description: |-
                        UID of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                lastDeleteRun:
                  description: LastDeleteRun specifies the time when the snapshots exceeding the retention period were deleted.
                  format: date-time
                  type: string
                lastDeletedBackups:
                  description: LastDeletedBackups contains the object references of the snapshots deleted during last run.
                  items:
                    description: ObjectReference contains enough information to let you inspect or modify the referred object.
                    properties:
                      apiVersion:
                        description: API version of the referent.
                        type: string
                      fieldPath:
                        description: |-
                          If referring to a piece of an object instead of an entire object, this string
                          should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                          For example, if the object reference is to a container within a pod, this would take on a value like:
                          "spec.containers{name}" (where "name" refers to the name of the container that triggered
                          the event) or if no container name is specified "spec.containers[2]" (container with
                          index 2 in this pod). This syntax is chosen only to have some well-defined way of
                          referencing a part of an object.
                        type: string
                      kind:
                        description: |-
                          Kind of the referent.
                          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                        type: string
                      name:
                        description: |-
                          Name of the referent.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      namespace:
                        description: |-
                          Namespace of the referent.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                        type: string
                      resourceVersion:
                        description: |-
                          Specific resourceVersion to which this reference is made, if any.
                          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                        type: string
                      uid:
                        description: |-
                          UID of the referent.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  type: array
                nextDeleteTimes:
                  additionalProperties:
                    type: string
                  description: |-
                    NextDeleteTimes contains the map of snapshot objects and
                    their expected deletion times (calculated based on MaxRetentionDays).
                  type: object
                nextRunTimes:
                  description: NextRunTimes contains the times when the next snapshot will be created.
                  items:
                    type: string
                  type: array
                schedule:
                  description: Schedule specifies the cron expression of the current active schedule.
                  type: string
                state:
                  description: State of the schedule.
                  type: string
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
