---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: alicloudredisclusters.cloud-resources.kyma-project.io
spec:
  group: cloud-resources.kyma-project.io
  names:
    categories:
    - kyma-cloud-manager
    kind: AlicloudRedisCluster
    listKind: AlicloudRedisClusterList
    plural: alicloudredisclusters
    singular: alicloudrediscluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: AlicloudRedisCluster is the Schema for the alicloudredisclusters
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AlicloudRedisClusterSpec defines the desired state of AlicloudRedisCluster.

              An AlicloudRedisCluster provisions a sharded AliCloud r-kvstore cloud-native
              cluster instance. See AlicloudRedisInstance for standard (non-sharded) HA.
            properties:
              authSecret:
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    maxProperties: 64
                    type: object
                  extraData:
                    additionalProperties:
                      type: string
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    maxProperties: 64
                    type: object
                  name:
                    maxLength: 253
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                    type: string
                    x-kubernetes-validations:
                    - message: name is immutable
                      rule: self == '' || oldSelf == '' || self == oldSelf
                type: object
              engineVersion:
                default: "5.0"
                description: EngineVersion is the Redis engine version. Immutable
                  after creation.
                enum:
                - "5.0"
                - "6.0"
                - "7.0"
                type: string
                x-kubernetes-validations:
                - message: engineVersion is immutable.
                  rule: (self == oldSelf)
              ipRange:
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
                x-kubernetes-validations:
                - message: IpRange is immutable.
                  rule: (self == oldSelf)
              parameters:
                additionalProperties:
                  type: string
                description: Parameters are passed to the AliCloud instance as runtime
                  configuration.
                type: object
              redisTier:
                description: |-
                  RedisTier defines per-shard capacity.
                  Total capacity = tier_memory × shardCount.
                enum:
                - C3
                - C4
                - C5
                - C6
                - C7
                type: string
              replicasPerShard:
                default: 0
                description: |-
                  ReplicasPerShard is the number of read-only replicas per shard.
                  All tiers currently exposed via redisTier map to proxy-based classes
                  (redis.logic.sharding.*), which always have ReadOnlyCount=0. This field
                  is reserved for future non-proxy tiers; it must be 0 for all current tiers.
                format: int32
                maximum: 1
                minimum: 0
                type: integer
                x-kubernetes-validations:
                - message: replicasPerShard must be 0; all current redisTier values
                    use proxy-based classes that do not support read replicas.
                  rule: (self == 0)
              shardCount:
                description: ShardCount is the number of data shards. Mutable via
                  ModifyShardingNum.
                format: int32
                maximum: 32
                minimum: 1
                type: integer
            required:
            - redisTier
            - shardCount
            type: object
          status:
            description: AlicloudRedisClusterStatus defines the observed state of
              AlicloudRedisCluster.
            properties:
              conditions:
                description: List of status conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              id:
                type: string
              state:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
