---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: sapnfsvolumesnapshotrestores.cloud-resources.kyma-project.io
spec:
  group: cloud-resources.kyma-project.io
  names:
    categories:
    - kyma-cloud-manager
    kind: SapNfsVolumeSnapshotRestore
    listKind: SapNfsVolumeSnapshotRestoreList
    plural: sapnfsvolumesnapshotrestores
    singular: sapnfsvolumesnapshotrestore
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.sourceSnapshot.name
      name: Source Snapshot
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.state
      name: State
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: SapNfsVolumeSnapshotRestore is the Schema for the sapnfsvolumesnapshotrestores
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SapNfsVolumeSnapshotRestoreSpec defines the desired state
              of SapNfsVolumeSnapshotRestore
            properties:
              destination:
                description: |-
                  Destination specifies where to restore the snapshot data.
                  Exactly one of ExistingVolume or NewVolume must be set.
                maxProperties: 1
                minProperties: 1
                properties:
                  existingVolume:
                    description: |-
                      ExistingVolume references an existing SapNfsVolume to revert in-place.
                      The snapshot must be the most recent snapshot of this volume.
                    properties:
                      name:
                        description: Name specifies the name of the SapNfsVolume resource.
                        type: string
                      namespace:
                        description: |-
                          Namespace specifies the namespace of the SapNfsVolume resource.
                          If not specified then namespace of the parent resource is used.
                        type: string
                    required:
                    - name
                    type: object
                  newVolume:
                    description: NewVolume defines a new SapNfsVolume to create from
                      the snapshot.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations to apply to the new SapNfsVolume
                          resource.
                        type: object
                      capacityGb:
                        description: |-
                          CapacityGb is the capacity in GiB for the new SapNfsVolume.
                          Must be >= the snapshot's source share size.
                        type: integer
                        x-kubernetes-validations:
                        - message: The field capacityGb must be greater than zero
                          rule: (self > 0)
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels to apply to the new SapNfsVolume resource.
                        type: object
                      name:
                        description: Name for the new SapNfsVolume resource.
                        type: string
                      namespace:
                        description: |-
                          Namespace for the new SapNfsVolume resource.
                          If not specified then namespace of the restore resource is used.
                        type: string
                    required:
                    - capacityGb
                    - name
                    type: object
                type: object
                x-kubernetes-validations:
                - message: Destination is immutable.
                  rule: (self == oldSelf)
              sourceSnapshot:
                description: SourceSnapshot references the SapNfsVolumeSnapshot to
                  restore from.
                properties:
                  name:
                    description: Name specifies the name of the SapNfsVolumeSnapshot
                      resource.
                    type: string
                  namespace:
                    description: |-
                      Namespace specifies the namespace of the SapNfsVolumeSnapshot resource.
                      If not specified then namespace of the parent resource is used.
                    type: string
                required:
                - name
                type: object
                x-kubernetes-validations:
                - message: SourceSnapshot is immutable.
                  rule: (self == oldSelf)
            required:
            - destination
            - sourceSnapshot
            type: object
          status:
            description: SapNfsVolumeSnapshotRestoreStatus defines the observed state
              of SapNfsVolumeSnapshotRestore
            properties:
              conditions:
                description: List of status conditions
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              createdVolume:
                description: CreatedVolume references the SapNfsVolume created (new-volume
                  restore only).
                type: string
              state:
                description: State of the restore operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
